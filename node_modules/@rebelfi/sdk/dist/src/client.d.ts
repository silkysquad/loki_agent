import { AxiosInstance } from 'axios';
import { RebelfiSDKConfig } from './types.js';
export declare function createHttpClient(config: RebelfiSDKConfig): AxiosInstance;
/**
 * Extract data from axios response.
 * The API returns data directly (no envelope wrapper).
 */
export declare function unwrapResponse<T>(response: {
    data: T;
}): T;
//# sourceMappingURL=client.d.ts.map