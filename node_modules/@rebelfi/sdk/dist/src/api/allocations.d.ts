import { AxiosInstance } from 'axios';
import { Allocation, AllocationListResponse, AllocationQuery, AllocationListRequest, EarningsQuery, EarningsResponse } from '../types.js';
export declare class AllocationsAPI {
    private readonly client;
    constructor(client: AxiosInstance);
    /**
     * List all allocations for a wallet.
     * Accepts AllocationListRequest (POST, supports walletId/userId) or
     * legacy AllocationQuery (GET, walletAddress only).
     */
    list(query: AllocationListRequest | AllocationQuery): Promise<AllocationListResponse>;
    /**
     * Get allocation by strategy ID
     * @param strategyId Strategy ID
     * @param wallet Wallet address or wallet ID
     */
    get(strategyId: number, wallet: string | number): Promise<Allocation>;
    /**
     * @deprecated Use get(strategyId, wallet) instead
     */
    getByVenue(venueId: number, walletAddress: string): Promise<Allocation>;
    /**
     * Get earnings history for a wallet with daily granularity
     */
    earnings(query: EarningsQuery): Promise<EarningsResponse>;
}
//# sourceMappingURL=allocations.d.ts.map